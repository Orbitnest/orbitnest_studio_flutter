name: orbitnest_studio_flutter_example
description: Minimal example for the orbitnest_studio_flutter package.
publish_to: none
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  orbitnest_studio_flutter:
    path: ../

flutter:
  uses-material-design: true
